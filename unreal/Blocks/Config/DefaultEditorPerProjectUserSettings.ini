[ContentBrowser]
ContentBrowserTab1.SelectedPaths=/Game

[/Script/UnrealEd.EditorPerProjectUserSettings]
bThrottleCPUWhenNotForeground=False

[/Script/UnrealEd.EditorExperimentalSettings]
; Async texture compilation fatals at TextureCompiler.cpp:327 on a cold DDC:
; AUnrealScene::BeginPlay -> UWeatherLib::initWeather loads the WeatherFX
; textures from inside FTextureCompilingManager::PostCompilation, and the
; re-entrant AddTextures trips the check. Synchronous compilation keeps the
; scene load off the async path.
bEnableAsyncTextureCompilation=False